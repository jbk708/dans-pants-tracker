{
  "name": "dans-pants-tracker",
  "version": "1.0.0",
  "description": "Asking the important questions, such as if Dan is wearing pants or shorts today.",
  "main": "index.js",
  "scripts": {
    "db:init": "node -e \"const db = require('./lib/db'); db.getDb(); console.log('DB initialized at data/tracker.db');\"",
    "dev": "netlify dev",
    "test": "echo \\\"No tests yet\\\""
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "keywords": ["pants", "tracker", "netlify"],
  "license": "MIT"
}